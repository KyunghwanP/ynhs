# 운영 노트

**앱을 실제로 굴리는 데 필요한 것**을 적어 둔 문서다. 구조·권한·주의점은
[`README.md`](README.md)에, 워커는 [`workers/README.md`](workers/README.md)에,
GAS는 [`gas/README.md`](gas/README.md)에 있다. 여기에는 **그 문서들에 없는 것** —
계정, 자동으로 도는 것, 정기적으로 해야 하는 일, 터졌을 때 보는 순서 — 만 적는다.

읽는 사람은 둘이다.

- **만든 사람 본인** — 반년 뒤의 자기 자신. 학년 초에 무엇을 올려야 하는지,
  터졌을 때 어디부터 보는지는 기억에 의존하면 반드시 샌다.
- **잠깐 대신 봐 줄 사람** — 자리를 비운 사이에 뭔가 멈췄을 때.

> 이 앱은 **개인 소유**다([`LICENSE`](LICENSE)). 이 문서는 소유권을 넘기기 위한
> 것이 아니라, 굴리는 데 필요한 것이 한 사람 머릿속에만 있지 않게 하려는 것이다.

---

## 0. 계정 — 누가 무엇을 쥐고 있나

이 앱은 **박경환 개인 소유**다. 학교 자산이 아니다([`LICENSE`](LICENSE) 참조).
계정이 개인 계정에 매여 있는 것은 **그렇게 하기로 한 것**이지 빠뜨린 것이 아니다.
전근·퇴직하면 앱은 만든 사람을 따라간다.

그러니 이 장은 "소유권을 넘겨라"가 아니라 **"무엇이 어디에 있고, 잠깐 자리를 비울
때 무엇이 막히나"**를 적는 자리다.

| 서비스 | 무엇이 걸려 있나 | 계정 |
|---|---|---|
| **Firebase** (`ynhs-7b5ba`) | 로그인·전 데이터·보안 규칙 | `pkh910518@yeungnam.hs.kr` |
| **Cloudflare** | 워커 3개(`*.pkh910518.workers.dev`)·R2 버킷 2개 | ⬜ 확인 필요 |
| **GitHub** (`KyunghwanP`) | 코드·배포(Pages)·자동화 | 개인 계정 |
| **Google Apps Script** | 주간교육활동·학사일정·상벌점·상담 | ⬜ 확인 필요 |
| **Gemini API** | 주간교육활동 AI 요약 | ⬜ 확인 필요 |
| **리로스쿨** | 상벌점 스크래핑 계정 | ⬜ 확인 필요 |

### 영구적 부재 — 의도대로

전근·퇴직하면 이 앱은 따라간다. 학교에 남길 이유가 없고, 남기지 않는 것이
[`LICENSE`](LICENSE)에 적힌 조건이다.

**다만 학교가 그 사실을 알고 있어야 한다.** 지금 시간표·성적·상벌점·상담 예약이
전부 여기를 지난다. 떠나는 날 갑자기 사라지면 곤란해지는 쪽은 학교다. 미리
알려 두면 학교가 대비할 시간이 생기고, 만든 사람도 뒷말을 안 듣는다.
기술 문제가 아니라 **말해 두는 문제**다.

### 일시적 부재 — 이건 따로 답이 필요하다

병가·출장·연수로 **2주쯤 자리를 비우는 사이에 앱이 터지면** 지금은 아무도 손을
못 댄다. 이건 소유권과 별개 문제라, 소유권을 넘기지 않고도 풀 수 있다.

가장 가벼운 순서대로:

1. **아무것도 안 한다** — 터지면 돌아와서 고친다. 며칠 멈춰도 학교가 견딜 수
   있다면 이게 제일 단순하다. **다만 그렇다는 것을 정해 두어야** 급할 때 서로
   당황하지 않는다.
2. **읽기만 열어 둔다** — Firebase 에 **뷰어** 권한으로 한 명. 데이터가 살아
   있는지 보고 "앱 문제인지 데이터 문제인지"까지는 알려 줄 수 있다. 아무것도
   못 망가뜨린다.
3. **되돌리기만 맡긴다** — 가장 흔한 사고는 '배포했더니 이상하다'인데, 되돌리기는
   GitHub 에서 `git revert` 한 번이다. 저장소에 **Collaborator 한 명**이면 된다.
   Firebase·Cloudflare 는 안 건드려도 된다.
4. **전부 맡긴다** — 소유자 추가. 지금 방침과 맞지 않는다.

> 3번이 값이 제일 크다. **사고의 대부분이 배포에서 나고, 되돌리기는 저장소만으로
> 된다.** Firestore 데이터와 워커는 건드릴 일이 거의 없다.

어느 쪽을 택하든 **정해서 적어 두는 것**이 핵심이다. 지금은 아무것도 안 정해져
있어서, 실제로 그 상황이 오면 그때 정하게 된다.

⬜ 정한 방식: _______________  ⬜ 맡은 사람: _______________

---

## 1. 처음 30분 — 읽는 순서

| 순서 | 문서 | 왜 |
|---|---|---|
| 1 | [`README.md`](README.md) 1~3장 | 구성·데이터·권한. 이걸 모르면 나머지가 안 읽힌다 |
| 2 | 이 문서 2·3장 | 무엇이 저절로 도는지, 비밀 값이 어디 있는지 |
| 3 | [`README.md`](README.md) 6장 | 건드릴 때 주의할 점. **두 저장소 동기화**가 핵심 |
| 4 | [`workers/README.md`](workers/README.md) 맨 앞 표 | 워커가 왜 셋인지 |
| 5 | [`tests/README.md`](tests/README.md) | 고치기 전에 돌려 볼 것 |

[`BACKLOG.md`](BACKLOG.md)에는 하다 만 것·하기로 한 것이 쌓여 있다. 급하지 않다.

### 한 장 요약

```
                   GitHub Pages
  KyunghwanP/ynhs ─────────────→ https://kyunghwanp.github.io/ynhs/   ← 선생님들이 쓰는 곳
  kyunghwanp/test ─────────────→ https://kyunghwanp.github.io/test/   ← 먼저 올려 보는 곳
         │                                    │
         │ Actions(test에서만)                │ 브라우저에서 직접
         ↓                                    ↓
    Firestore (ynhs-7b5ba) ←──────────── Firebase Auth (구글 로그인, @yeungnam.hs.kr)
         ↑          ↑
         │          └── Cloudflare Workers ── R2  ynhs-photos   (학생 사진)
         │                teacher-api            ynhs-notices   (공지·메모 그림)
         │                notice-api
         │                consult-api  ←── 학부모 (parent.html)
         │
         └── Google Apps Script ── 구글 사이트(주간교육활동) · 시트(학사일정·상벌점)
```

**코드를 고치는 곳은 사실상 `index.html` 하나다**(약 23,000줄). 나머지는 주변 도구다.

---

## 2. 저절로 도는 것

자동화 워크플로는 **`test` 저장소에서만** 돈다. Firestore 에만 쓰기 때문에
운영(`ynhs`) 배포가 그대로 받아 간다. 양쪽에 두면 중복 실행이 된다.

| 워크플로 | 언제 | 하는 일 | 멈추면 |
|---|---|---|---|
| `fetch-weather` | 30분마다 | 날씨 → Firestore | 현황판 날씨가 굳는다 |
| `sync-grades` | 매일 05:30 | 모의고사 성적 동기화 | 성적 탭이 옛 회차에 멈춘다 |
| `scrape` | 06:00~08:50 사이 여러 번 | 주간교육활동 구글 사이트 긁기 | 주간교육활동이 지난주에 멈춘다 |
| `Summarize` | 매일 08:00 | 위 결과를 Gemini 로 요약 | 요약만 안 바뀐다(원본은 뜬다) |
| `riro-points` | 매일 02:00 | 리로스쿨 상벌점 긁기 | 상벌점이 안 쌓인다 |

*시각은 전부 한국 시각. 워크플로 파일에는 UTC 로 적혀 있다.*

빌드 워크플로(`build-widget`·`build-app-ahk`·`build-desktop`)는 **양쪽 저장소에
있고**, 해당 폴더가 바뀔 때만 돈다. 릴리스에 exe 를 올리며, 런처가 그것을 받아 간다.

**확인하는 곳**: GitHub → Actions 탭. 빨간 줄이 며칠 이어지면 대개 아래 둘 중 하나다.
- 서비스 계정 키 만료·회수 → `FIREBASE_SERVICE_ACCOUNT` 다시 넣기
- 긁어 오는 쪽(구글 사이트·리로스쿨)의 화면이 바뀜 → `scripts/` 의 해당 파일 수정

또 하나, GAS 가 **`triggerWeeklySummary`** 로 `scrape` → `Summarize` 를 연달아
부르는 길이 있다(앱의 '새로 받기'가 이걸 쓴다). GAS 스크립트 속성의
`GITHUB_TOKEN` 이 만료되면 이 길만 조용히 죽는다.

---

## 3. 비밀 값이 사는 곳

**저장소에는 하나도 없다.** 아래 네 곳에만 있고, 넷 다 한 번 넣으면 다시 볼 수 없다
(가려진 채로만 보인다). **원본은 따로 보관해야 한다.**

| 사는 곳 | 이름 | 무엇 |
|---|---|---|
| GitHub Secrets (`test`) | `FIREBASE_SERVICE_ACCOUNT` | Firestore 쓰기용 서비스 계정 JSON |
| | `GEMINI_API_KEY` | AI 요약 |
| | `RIRO_ID` · `RIRO_PW` | 리로스쿨 로그인 |
| Cloudflare (`consult-api`) | `SA_JSON` | 서비스 계정 JSON |
| Cloudflare (`teacher-api`) | `SA_JSON` | 〃 (**같은 값을 따로 넣은 것**) |
| Apps Script 속성 | `GITHUB_TOKEN` | 워크플로 실행 |
| | `FIREBASE_SA_JSON` · `FIREBASE_PROJECT_ID` | Firestore REST |

`notice-api` 에는 비밀 값이 없다. Firestore 를 안 건드리기 때문이다.

**공개해도 되는 값**(Secret 아님, 그냥 Text 변수로 둔다):
`FIREBASE_API_KEY`(웹 API 키는 원래 공개용, `index.html` 에도 있다),
`ALLOWED_ORIGINS`(`https://kyunghwanp.github.io`).

> 서비스 계정 키는 **하나에 여러 개**를 만들 수 있다. 새로 발급해도 기존 키는
> 살아 있으니, 값을 잃어버렸으면 새로 발급해서 넣으면 된다.

---

## 4. 사람이 해야 하는 일

앱이 스스로 못 하는 것들. **때를 놓치면 학기 초에 앱이 텅 빈 것처럼 보인다.**

### 학년 초 (제일 바쁜 때)

`upload.html`(test 저장소에만 있다)로 올린다. 순서가 중요하다.

1. **시간표** → `appdata/main`. 다른 게 다 여기에 매여 있다
2. **운영표**(요일 대체·창체·시험) → `appdata/operating`
3. **교원 명렬** 🪪 → `acl/emailByName`. **이걸 안 하면 권한이 안 붙는다.**
   시간표에는 수업 없는 교장·교감이 아예 없어서, 이름→계정 해석이 여기에 의존한다
4. **학생 명렬** → `students/main` · `studentsContact/main`
5. **선택과목 편성** → `electives/{학년-반}`
6. **사진 명렬** 🪪 → Firestore(작은 것) + R2(고화질) 동시에

### 학기마다 / 필요할 때

- **학부모 상담 슬롯** — 교사 앱에서 각자 연다
- **전교생 성적 열람 권한** — `acl/gradeManual.managers` 에 이메일을 직접 넣고 뺀다.
  **보안 규칙이 직접 읽으므로 즉시 반영**된다(동기화를 기다릴 필요 없다)
- **학사일정 시트** — GAS 가 읽어 간다. 시트를 갈아엎지 말 것

### 한 번만 해 두면 되는 것 (아직 안 한 것)

- [ ] **Firestore TTL 정책** — 컬렉션 `accessLogs`, 필드 `expireAt`.
      안 만들어도 돌지만 연락처 조회 기록이 계속 쌓인다
- [ ] **`adiga-pdf-proxy` 에 도메인 제한** — 아무 주소나 중계하는 **열린 프록시**다.
      남이 발견하면 무료 한도를 갉아먹는다
- [ ] **자리 비울 때 어떻게 할지 정하기** — 0장. 정하지 않는 것도 답이지만,
      정하지 않았다는 것을 적어 두어야 한다

---

## 5. 터졌을 때 — 5분 안에 보는 곳

[`README.md`](README.md) 7장에 증상별 표가 있다. 그 앞에 **어느 층이 죽었는지**부터
가른다.

| 증상 | 어느 층 | 먼저 볼 곳 |
|---|---|---|
| 아무도 못 들어옴 | 배포 | GitHub → Actions → `pages build and deployment` |
| 로그인은 되는데 화면이 텅 빔 | Firestore | 콘솔에서 `appdata/main` 이 있는지 |
| 특정 기능만 죽음 | 워커 | `workers.dev` 주소를 주소창에 → `ORIGIN` 이 뜨면 살아 있음 |
| 사진·그림만 안 뜸 | R2 바인딩 | 워커 Settings → Bindings (`PHOTOS` / `NOTICES`) |
| 데이터가 어제에 멈춤 | Actions | GitHub → Actions 에 빨간 줄 |
| 한 사람만 이상함 | 권한 | `acl/emailByName` 에 이름이 있는지, 표기가 같은지 |
| 배포했는데 반영이 안 됨 | 서비스워커 | 주소 뒤에 `?nosw=1` — 이러면 워커를 건너뛰고 등록도 푼다 |

**워커가 의심되면** Cloudflare → 해당 워커 → Logs → *Begin log stream* 을 켜고
다시 해 본다. 원인이 그대로 찍힌다.

**`?nosw=1`** 은 기억해 둘 만하다. 서비스워커가 화면을 못 띄우는 상황에서
개발자도구 없이 빠져나오는 유일한 길이다.

---

## 6. 되돌리는 법

| 무엇 | 되돌리기 | 걸리는 시간 |
|---|---|---|
| 앱(웹) | `git revert` → push | 1~2분(Pages 빌드) |
| 워커 | 저장소의 옛 `workers/*.js` 를 대시보드에 붙여넣고 Deploy | 몇 분 |
| 그림 기능만 끄기 | 워커에서 R2 바인딩(`NOTICES`) 삭제 | 즉시. 글은 그대로 뜬다 |
| 학생 사진 고화질만 끄기 | `PHOTOS` 바인딩 삭제 | 즉시. 작은 사진으로 돌아간다 |
| 상담 예약을 GAS 로 | `parent.html` 의 `USE_WORKER` → `false` | 배포 시간 |
| 보안 규칙 | Firebase 콘솔에 옛 규칙 붙여넣기 | 즉시 |

워커는 **저장소에 있는 파일이 곧 원본**이라 되돌릴 코드는 항상 있다. `git log
workers/notice-api.js` 로 옛 버전을 꺼내 붙여넣으면 된다.

> Cloudflare 대시보드에 **Deployments** 탭이 있어 옛 버전으로 바로 되돌릴 수 있을
> 것이다. 이 방식은 아직 실제로 써 본 적이 없으니, 급하지 않을 때 한 번 눌러 보고
> 되면 이 문서를 고쳐 둘 것. 된다면 붙여넣기보다 훨씬 빠르다.

**Firestore 데이터는 되돌릴 수 없다.** 잘못 업로드하면 원본 엑셀로 다시 올리는
수밖에 없다. 업로드 전 원본 파일을 보관할 것.

---

## 7. 돈

전부 **무료 한도 안에서** 돌게 만들어져 있다. 지금 얼마나 쓰고 있는지는 각 콘솔의
사용량(Usage) 화면에서 봐야 한다 — 아래 표의 '한도'는 규격이고, '지금'은 아무도
재 본 적이 없다.

| | 무료 한도 | 넘으면 |
|---|---|---|
| Firestore | 읽기 5만/일 | 그날 읽기가 막힌다 → 앱이 텅 빈 것처럼 보인다 |
| Cloudflare Workers | 10만 요청/일 | 요청이 거부된다 → 사진·연락처·그림이 죽는다 |
| R2 저장 | 10GB | 쓰기가 막힌다 (사진 600명이 약 54MB니 여유가 크다) |
| R2 전송 | **무료** | — |
| GitHub Actions | 공개 저장소 무제한 | — |
| Gemini API | 무료 등급 | 요약만 안 된다 (원본 주간교육활동은 뜬다) |

**Firestore 읽기가 가장 먼저 닿을 곳이다.** 전 교사가 매일 여러 번 열고, 현황판
한 번에 여러 문서를 읽는다. 갑자기 느려지거나 화면이 비면 **콘솔의 사용량부터**
본다 — 한도에 걸린 것과 코드가 깨진 것은 증상이 똑같다.

> 한 번쯤 Firebase·Cloudflare 콘솔에서 실제 사용량을 재 보고 이 표에 적어 두면,
> 다음 사람이 '여유가 있나'를 추측하지 않아도 된다.

---

## 8. 손대기 전에 알아 둘 것

솔직하게 적는다.

**코드는 한 파일에 몰려 있다.** `index.html` 이 23,000줄이다. 혼자 만들고 혼자
고치는 동안은 이쪽이 빨랐다. 여러 명이 붙게 되면 이 결정부터 다시 봐야 한다.

**검사는 믿어도 된다.** `tests/` 의 검사들은 `index.html` 에서 **실제 코드를 떼어내**
돌린다. 로직을 옮겨 적은 것이 아니라서, 통과하면 진짜로 도는 것이다.
고치기 전과 후에 돌려 볼 것.

**test 저장소에는 진짜 선생님들이 있다.** 시험대이긴 하지만 베타로 쓰는 분들이
있어서, 화면이 바뀌는 배포는 미리 알리는 것이 좋다.

**위험한 자리 셋**
1. **청소(`sweep`)** — 실제로 사고가 났다. 옛 워커가 새 요청을 반쯤 알아들어
   공지 사진을 전부 지웠다. 그래서 새 동작에는 **반드시 새 이름**을 준다.
   옛 워커가 모르면 아무 일도 안 하도록.
2. **업로드** — 시간표를 올리면 교사 이메일이 사라진다(엑셀에 없으니까).
   보존 코드가 `upload.html` 에 있다. 건드리지 말 것.
3. **두 저장소 동기화** — 배포 경로(`/test/` ↔ `/ynhs/`)를 바꿔야 하는 파일이
   있다. [`README.md`](README.md) 의 「두 저장소 동기화」를 따를 것.

> 덧붙여, **두 저장소의 `README.md` 가 서로 어긋나 있다.** ynhs 쪽에는 배포 경로를
> 바꿔야 하는 파일이 표로 정리돼 있는데 test 쪽에는 두 줄뿐이다. 같은 문서가
> 한쪽만 자란 것이니, 눈에 띄면 맞춰 둘 것.

**안 고쳐도 되는 것으로 정한 것**은 [`BACKLOG.md`](BACKLOG.md) 에 이유와 함께
적혀 있다. 다시 꺼내기 전에 거기부터 볼 것.
