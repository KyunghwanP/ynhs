# 보류 목록 — 논의했지만 하지 않기로 한 것들

작업하면서 "이렇게 하면 좋겠다"고 나왔지만 **일부러 안 한 것들**을 모아 둔다.

목적은 목록 자체가 아니라 **판단 근거를 남기는 것**이다. 반년 뒤에 같은 얘기가 다시
나왔을 때 처음부터 다시 따지지 않도록, *왜* 안 했는지와 *어떤 조건이 바뀌면 다시
봐야 하는지*를 같이 적는다.

각 항목의 상태:

- **안 하기로 함** — 따져 보고 안 하는 게 낫다고 판단
- **미룸** — 필요해지면 하기로, 지금은 아님
- **결정 안 함** — 얘기만 나오고 결론이 없음. 언젠가 판단해야 함

---

## 🔴 아직 판단이 필요한 것

### 방학에 "수업 5분 전" 알림이 갈 수 있다 — **결정 안 함**

다음 수업 알림(`startNextClassNotifyWatch`)이 운영표에 없는 날에는 **기초 시간표로
떨어진다.**

```js
const slot = op ? ttMaskTeacherSlot(op, base, pulled && pulled[b.period], wd, b.period) : base;
//                                                                                        ↑ op 없으면 정규표
```

시간표 **화면**은 같은 상황을 반대로 처리한다.

```js
if(!op) return null;   // 운영표에 없는 날(방학·휴업 등) → 빈칸
```

방학 날짜는 운영표에 아예 없는 것으로 확인했다. 즉 방학 중 앱이 열려 있으면
"1교시 수업 5분 전"이 뜬다. 지금은 앱을 켜 둬야 하니 잘 안 드러나지만,
아래 '웹푸시' 항목대로 서버로 옮기면 **방학 내내 매일 울린다.**

- **하려면**: 알림도 화면과 같게 — 운영표 레코드가 없으면 보내지 않는다 (10분, 테스트 포함 30분)
- **판단할 것**: 그러면 **운영표 업로드가 늦은 평일에는 알림이 안 간다.** 방학 오발사와
  업로드 지연 시 침묵 중 무엇이 나은지. 운영표를 주 단위로 미리 올린다면 후자는 거의 안 생긴다
- **재검토 신호**: 방학 중 알림을 받았다는 얘기가 나오면 즉시. 웹푸시를 붙이기 전에는 **반드시**

### adiga-pdf-proxy 오픈 프록시 제한 — **결정 안 함**

`mincut-extract.html`·`appguide.html`이 쓰는 `adiga-pdf-proxy` 워커는 **아무 URL이나
중계한다.** 주소가 공개 저장소 두 곳에 박혀 있고, 계정에 결제 수단도 등록돼 있다.

누구든 이걸 무료 익명 프록시로 쓸 수 있고, **그 트래픽은 계정 앞으로 잡힌다.**
이 문서의 다른 항목과 달리 **실제 금전 위험이 있는 유일한 항목**이다.

- **하려면**: 허용 도메인을 `adiga.kr` 계열로 제한 + `ALLOWED_ORIGINS` 검사 추가
  (`consult-api`가 쓰는 `corsHeaders()`를 그대로 가져다 쓰면 된다)
- **예상**: 20분
- **재검토 신호**: Cloudflare 사용량 알림, 청구서에 예상 밖 금액

### 명렬 위성 데이터에 고유 학생 ID가 없다 — **결정 안 함**

사진·선택과목·성적·상벌점이 모두 **`{학년-반}` 문서 + `students[번호]`** 로만 붙는다.
이름을 대조하지 않는다.

```js
const key = `${parseInt(s.grade)}-${parseInt(s.room)}`;   // 사진·성적·선택과목 공통
const st  = 반문서.students[String(parseInt(s.num))];
```

전입·전출이나 반 재편성으로 번호가 바뀌면 **그 번호를 새로 받은 학생에게 옛 데이터가
붙는다.** 오류가 나지 않고 조용히 잘못 붙는 것이라, 눈치채기 어렵다.

- **가벼운 방책**: 번호로 찾은 뒤 **이름이 같은지 한 번 더 확인**하고 다르면 표시하지
  않는다. 데이터 구조를 안 건드리고 오배정만 막는다 (반나절)
- **근본 해결**: 명렬에 **학번 열**을 추가하고 위성 데이터를 학번으로 붙인다.
  업로드 양식 변경 + 기존 데이터 이관이 필요해 **방학 중** 작업 (2~3일)
- **재검토 신호**: 학기 중 반 이동이 실제로 생겼을 때, 또는 "사진이 다른 학생 것"
  같은 문의가 한 번이라도 나오면 즉시

### 교원·학생 연락망 PC 레이아웃 — **결정 안 함**

카드가 한 줄에 하나씩 쌓이는 모바일 레이아웃이 PC에서 그대로 늘어난다. 1920px에서는
이름과 오른쪽 버튼 사이가 통째로 빈다. 60명이면 세로로 4,000px쯤 내려가야 한다.

- **A안(가벼움)**: `.contacts-list` 를 `grid-template-columns:repeat(auto-fill,minmax(300px,1fr))`
  로. CSS 6줄, 1600px에서 5열이 되어 12줄로 줄어든다. 모바일은 그대로
- **B안(정보량)**: PC만 표로 — **이름 · 부서 · 내선 · 휴대폰**. 지금 카드에 없는
  **내선번호가 목록에 바로** 보인다. 마크업을 새로 짜고 모바일은 카드로 분기해야 함
- **예상**: A 20분 / B 반나절
- **재검토 신호**: 연락망을 PC에서 자주 쓴다는 얘기가 나오면

---

## 보안 — 안 하기로 한 것

### `studentPhotos` 쓰기를 관리자만으로 — **안 하기로 함**

지금은 `allow read, write: if isYnhsTeacher()` 라 아무 교사나 학생 사진 문서를
덮어쓰거나 지울 수 있다. 사진을 올리는 건 `upload.html`(관리자)뿐인데도.

**왜 안 했나** — 실수로는 일어나지 않는다. 앱(`index.html`)은 사진을 **읽기만** 하고,
업로드는 `{ merge: true }`라 일부만 올려도 나머지가 남는다. 고의로 지우려면 브라우저
콘솔에서 Firebase SDK를 다시 초기화해야 하는데(5줄 정도), **그럴 능력과 동기를 동시에
갖춘 동료**를 전제해야 성립한다. 그 전제를 깔면 어차피 동아리·특별실·주간계획도 같이
지울 수 있어서 이것만 잠근다고 달라지는 게 없다.

- **하려면**: `allow read: if isYnhsTeacher(); allow write: if isAppAdmin();`
- **예상**: 5분 (규칙 테스트 포함 15분)
- **재검토 신호**: 규칙을 어차피 손볼 일이 생기면 끼워 넣기. 실제 사고가 한 번이라도
  나면 즉시

### `consultationsPublic` 쓰기를 담임·관리자만으로 — **안 하기로 함**

원본 `consultations`는 담임만으로 잠겨 있는데 **미러는 전 교사에게 열려 있다.**
조건이 서로 다른 상태다.

**왜 안 했나** — 처음엔 위험해 보였지만 확인해 보니 **부정 예약도 정보 유출도 안 된다.**
워커가 예약을 처리할 때 미러가 아니라 **원본을 기준으로** 판단하기 때문이다:

```js
const { data } = await fsGet(env, 'consultations', classKey);   // ← 원본
if (openAt && new Date(openAt).getTime() > Date.now()) return { error: 'NOT_OPEN' };
if (idx === -1) return { error: 'GONE' };
```

미러를 조작하면 **학부모 화면 표시만** 어그러진다(슬롯이 안 보이거나, 눌러도 거부됨).
게다가 담임이 상담 화면을 열어두면 `syncConsultPublic`이 원본과 맞춰 **자동 복구**한다.
슬롯 개설은 트랜잭션이라 원본이 막히면 미러도 같이 실패하므로 실수로는 안 깨진다.

- **하려면**: `allow write: if isAppAdmin() || isHomeroomOf(classKey);`
  (원본과 **글자 그대로 같은 조건**이 된다)
- **예상**: 10분
- **재검토 신호**: **상담 기능을 확장할 때 반드시 같이 볼 것.** 조건이 서로 다르면
  한쪽만 고치고 빠뜨리기 쉽다. 넓힐 때는 두 규칙이 같은 함수를 보게 만들 것

### 공용 컬렉션 쓰기 제한 — **안 하기로 함**

`appdata` · `weeklyData` · `clubs` · `electives` · `roomBookings` · `docsSheets` 는
전 교사 읽기/쓰기다.

**왜 안 했나** — **설계상 맞다.** 여러 교사가 같이 편집하는 자료다. 지금 좁히면 협업이
막힌다. 한 명의 실수로 날아갈 수 있고 누가 지웠는지 알 방법이 없다는 약점은 있지만,
그건 권한이 아니라 백업으로 다룰 문제다.

---

### 다른 교사의 할 일이 브라우저로 내려온다 — **미룸**

앱이 **모든 교사의 `tasks/{uid}/items` 를 통째로 구독**한 뒤 브라우저에서 거른다:

```js
onSnapshot(collection(fbDb, 'tasks', teacher.uid, 'items'), snap => {
  const sharedTasks = snap.docs.map(...)
    .filter(t => (t.sharedWith || []).some(s => myIdentifiers.includes(s)));  // ← 클라이언트에서
```

화면에는 공유받은 것만 보이지만 실제로는 **전 교사의 할 일이 전부** 내려와 있다.
규칙도 이를 허용한다(`tasks` 읽기 = `isYnhsTeacher()`). `memos`·`memos2` 는
`uid == userId` 로 제대로 막혀 있는데 **할 일만 다르다.**

2026-08 연락처 작업에서 고친 것과 **구조가 같다**(화면에 안 보이는 것까지 내려보냄).

**왜 미뤘나 — 두 가지다.**

1. **성격이 다르다.** 연락처는 학생·학부모 개인정보였고, 이건 동료의 업무 할 일
   제목이다. 법적으로도 실질적으로도 층위가 다르다.
2. **고치다 공유가 깨질 수 있다.** 규칙으로 서버에서 거르려면
   `resource.data.sharedWith.hasAny([request.auth.token.email])` 형태여야 하는데,
   `sharedWith` 에는 **이메일 또는 이름**이 들어간다. 이름은 토큰에 없고, 있어도
   본인이 바꿀 수 있어 권한 판단에 쓸 수 없다. 코드에 이름→이메일 자가 치유가
   있지만 **일부러 못 푸는 이름은 남겨둔다**:

   ```js
   // 풀리지 않는 이름(퇴직·전출·명렬 미등록)은 그대로 둔다 — 지우면 조용한 공유 해제가 된다
   ```

   규칙으로 막으면 그 항목들이 **조용히 공유 해제된다.** 매일 쓰는 기능에서 그건
   유출보다 나쁘다.

- **하려면**: ① `sharedWith` 를 이메일로 통일(잔여 이름 처리 방침 결정) →
  ② 클라이언트를 `where('sharedWith','array-contains', 내이메일)` 쿼리로 →
  ③ 규칙에 `hasAny([request.auth.token.email])` 추가 → ④ 공유 기능 전체 재검증
- **예상**: 2~3시간 (처음엔 1시간으로 봤으나 잘못 짚었다)
- **재검토 신호**: 할 일에 개인적인 내용을 적는다는 얘기가 나올 때,
  또는 공유 기능을 어차피 손볼 일이 생겼을 때

### 학부모 Firebase Auth 계정이 계속 쌓인다 — **미룸**

학부모가 자녀 정보 확인을 통과할 때마다 `p_{학년-반}_{번호}` UID로 **Firebase Auth
계정이 하나 생긴다.** 지우는 로직이 없어 학년이 올라가도 남고, 내년이면 같은 UID가
다른 학생을 가리킨다.

**왜 미뤘나** — 위험하지 않다. 이 계정으로 읽을 수 있는 것은 개인정보가 없는
`consultationsPublic/{자기 반}` 뿐이고, 학생당 하나라 늘어나는 속도도 느리다.
콘솔이 지저분해지는 것 외에 실질적인 해는 없다.

- **안 쌓이게 하려면**: `parent.html`의 `exchangeToken()` 호출을 빼면 `idToken`이
  `null`이 되어 **이미 들어 있는 폴백**(워커의 `getOpenSlots`)으로 넘어간다.
  계정이 아예 안 생긴다
- 직접 읽기를 넣은 이유는 "오픈 시각 폭주를 서버 없이 감당"인데, 카운트다운은
  로컬에서 돌고 요청은 오픈 순간 **1인당 한 번**이다. 한 반 30명, 전교가 동시에
  열려도 1,000건 수준 — 워커 무료 한도(10만/일)에 한참 못 미친다
- **예상**: 10분 (기존 계정 정리는 콘솔에서 별도)
- **재검토 신호**: 계정이 수백 개로 늘거나, 학년이 바뀌어 UID가 겹칠 때

### GAS 상담 배포 해제 — **결정 안 함**

상담 백엔드를 워커로 옮기면서 되돌릴 여지를 두려고 GAS 배포를 남겨 뒀다
(`parent.html` 의 `USE_WORKER` 를 `false` 로 되돌리면 그쪽으로 간다).

워커가 안정적으로 돌고 있으므로 이제 정리할 때다. **살아 있는 동안은 그 URL로
상담 데이터에 닿을 수 있고**, 검증이 워커보다 느슨했을 가능성이 있다.

- **하려면**: Apps Script → 배포 관리 → 보관처리. `parent.html` 의 폴백 경로도 함께 정리
- **재검토 신호**: 없음. 그냥 하면 된다

### 저장소를 비공개로 — **안 하기로 함**

코드를 베껴 가는 게 신경 쓰여 검토했다. 비공개 저장소 자체는 무료지만
**GitHub Pages 는 무료 플랜에서 공개 저장소에서만 게시된다.** 비공개로 돌리면
`kyunghwanp.github.io/ynhs/` 가 내려가 앱도 위젯도 죽는다. Pro 이상이 필요하다.
Actions 도 공개는 무제한이지만 비공개는 월 할당량제이고, 워크플로 셋이 전부
`windows-latest` 라 **분 소모가 2배**다.

**그리고 조용히 깨지는 것이 하나 더 있다.** 런처가 익명으로 GitHub API 를 부른다.

```ahk
; widget/ynhs-launcher.ahk:16 · app/ynhs-app-launcher.ahk:14  (주소는 레포마다 다름)
global RAW := "https://api.github.com/repos/{소유자}/{저장소}/contents/widget/ynhs-widget.ahk?ref=main"
```

`Authorization` 헤더가 없다. 비공개가 되면 404 → **전 교사의 위젯·앱 자동 업데이트가
멈춘다.** 토큰을 넣을 수도 없다 — exe 에 박으면 뜯어서 꺼낼 수 있어 비공개 저장소
열쇠를 배포하는 꼴이다. Releases 방식으로 바꿔야 한다.

**왜 안 했나 — 얻는 게 거의 없다.** 앱이 단일 파일이라 배포된 사이트에서 F12 만 눌러도
`index.html` 전문이 나온다. 그게 곧 소스다. `.ahk` 도 런처가 평문으로 내려받아 사용자
PC 에 떨어진다. 비공개가 실제로 가리는 것은 **결과물이 아니라 만든 과정**(커밋 이력·
테스트·워크플로·문서)뿐이다.

`LICENSE` 는 이미 `All Rights Reserved` 로 박혀 있다. **공개 = 이용허락이 아니다.**
GitHub 에서 보고 포크하는 것까지가 ToS 범위이고, 그 이상 가져다 쓰면 저작권 침해라
DMCA takedown 을 걸 수 있다.

- **그래도 하려면**: ① 런처를 Releases 방식으로 먼저 바꾼다 → ② Pro 로 올린다 →
  ③ 비공개 전환. **①을 건너뛰면 자동 업데이트가 조용히 멈춘다**
- **가벼운 대안**: `README` 맨 위에 이용조건 명시(지금은 `LICENSE` 파일에만 있어 잘 안 보인다),
  `index.html` 상단 주석과 화면에 저작권 표시 — 통째로 베낀 것이 발견됐을 때 근거가 된다
- **재검토 신호**: 실제로 베껴 간 사례를 확인했을 때

## 보안 — 못 하는 것으로 결론 난 것

### 스크린샷 차단 — **불가능**

PWA·웹에는 그런 API가 **아예 없다.** 스크린샷은 OS가 찍고, 브라우저 안의 JS는 그
사실을 알 수도 막을 수도 없다.

- 안드로이드 `FLAG_SECURE`, iOS `userDidTakeScreenshotNotification` — **네이티브 전용**
- PWA로 설치해도 동일. 껍데기만 앱이고 렌더링은 그대로 브라우저
- `blur`/`visibilitychange`로 가리기, 우클릭 차단, PrintScreen 가로채기 — **전부 가짜.**
  안드로이드 스크린샷은 blur를 안 내고, iOS는 이미 찍힌 뒤에 발생한다
- DRM(EME)으로 검게 만드는 건 `<video>` 안의 보호 영상에만 적용된다

유일한 진짜 방법은 WebView로 감싼 네이티브 앱에 `FLAG_SECURE`를 거는 것인데
(TWA는 브라우저 프로세스라 안 됨), 안드로이드만 되고 스토어 배포·유지보수가 붙는다.
그리고 **옆에서 다른 폰으로 찍는 건 어차피 못 막는다.**

### 워터마크 · 인쇄 차단 — **결정 안 함**

막는 대신 **추적**하는 쪽. 화면 위에 열람자 이메일·시각을 옅게 깔면 스크린샷에 같이
찍혀 유출 시 출처가 드러나고, 그래서 애초에 안 찍게 된다.

- **하려면**: CSS 몇 줄. 위젯·인쇄에서는 빼야 함.
  민감 화면(학생 종합검색·성적·학부모상담)만 걸지, 앱 전체에 걸지 정해야 함
- **인쇄 차단**은 진짜로 된다 — `@media print`로 민감 영역 `display:none`
- **예상**: 워터마크 30분, 인쇄 차단 10분
- **재검토 신호**: 실제 유출 사고, 또는 감사 대응 요구

---

## 편의 기능 — 미룬 것

### 앱을 꺼 놔도 오는 알림(웹푸시) — **미룸**

지금 알림은 **페이지가 띄운다.** `initNotify()` 로 권한을 받고, 언제 띄울지는
`startConsultNotifyWatch` · `startTaskNotifyWatch` · `startNextClassNotifyWatch` 가
페이지 안에서 판단한다. 탭을 닫으면 같이 죽는다.

웹푸시는 아직 안 붙어 있다. `sw.js` 에 `notificationclick` 은 있지만 **`push` 핸들러가
없고**, `PushManager` · VAPID 는 코드 어디에도 없다.

**두 단계로 나뉘고, 난이도가 많이 다르다.**

**1단계 — 상담·업무 알림.** 쉽다. 알림이 필요한 순간이 '누가 글을 쓸 때'라
저장 시점에 워커를 한 번 부르면 끝난다. Firestore 를 주기적으로 훑을 필요가 없어
**읽기 사용량이 안 는다.** 여기서 배관(VAPID 키 · 구독 저장 · `push` 핸들러)을 다 깐다.

**2단계 — 시간표 알림.** 시각 기반이라 서버가 시계를 대신 봐야 한다. 종이 고정이라
폴링은 필요 없고 Cron 으로 정확히 맞출 수 있다. `BELL_SCHEDULE` 시작 5분 전 =
KST 08:25 09:25 10:25 11:25 13:25 14:25 15:25. 크론은 UTC 라 1교시는 요일까지 밀린다.

```
25 23 * * 0-4            # 1교시 — UTC 일~목 = KST 월~금
25 0,1,2,4,5,6 * * 1-5   # 2~7교시
```

읽기량은 깨울 때마다 `appdata/main` + `appdata/operating` 2건, 하루 14건이라 무시할 수준.

**2단계의 진짜 비용은 크론이 아니라 로직 중복이다.** 워커가 "이 교사 6교시에 수업
있나"를 알려면 `ttOpForDate` → `ttResolveCellOp` → `ttTeacherDay` → `ttMaskTeacherSlot`
+ `TT_ACT`/`TT_NOCLASS`/`TT_EXAM` 을 그대로 들고 있어야 한다. **이 로직은 2026-08 에
같은 버그를 두 번 냈다** — `ttDayCross` 게이트가 `ttTeacherDay` 와 `ttTeacherOp` 에
각각 있어서 한쪽만 고쳤고, 8/26 동아리 이동이 교사 화면에만 안 나왔다.
워커에 복사하면 **세 번째 자리**가 생긴다.

- **한 벌로 만들기**: `shared/operating.mjs` 로 빼서 앱과 워커가 같이 쓴다.
  `index.html` 이 파일을 하나 더 받게 되고 `sw.js` PRECACHE 에도 넣어야 한다.
  워커가 import 하려면 **빌드 단계가 필요하다** — 아래 '워커 자동배포' 가 선행 조건
- **두 벌 + 대조 테스트**: 같은 자료를 양쪽에 넣어 결과가 같은지 본다. 훨씬 싸고
  지금 테스트 방식과 맞는다. 첫 판은 이쪽

**같이 처리해야 하는 것 두 가지**

1. **중복 알림.** 앱이 열려 있으면 앱과 서버가 둘 다 보내 **두 번 뜬다.** 지금
   `showNotification(title, { body, icon, badge })` 에 `tag` 가 없어 쌓인다.
   양쪽이 `tag: 'class:2026-08-26:6'` 같은 같은 값을 주면 하나만 남는다
2. **방학 오발사.** 위 🔴 항목을 먼저 정리하지 않으면 방학 내내 매일 울린다

**아이폰 제약** — 사파리 탭으로 열어 둔 상태로는 안 온다. **홈 화면에 추가**한
PWA 여야 한다(iOS 16.4+). 안드로이드·데스크톱은 브라우저 상태로도 온다.
아이폰 쓰는 교사에게는 설치 안내가 따라야 실제로 받는다.

**돈은 안 든다.** VAPID 웹푸시는 무료고 이미 있는 워커에 얹으면 된다.
Firebase Blaze 로 올릴 필요 없다.

- **예상**: 1단계 반나절 / 2단계 1~2일(로직 중복 처리 방식에 따라)
- **재검토 신호**: "상담 신청이 온 걸 몰랐다" 같은 얘기가 반복될 때.
  데스크톱은 AHK 위젯이 이미 토스트로 커버하므로, 아쉬운 쪽은 **모바일**이다

### 계정이 두 개인 교사 지원 (`acl/emailAliases`) — **미룸**

권한이 전부 **이메일**에 묶여 있다. 명렬의 ID 칸으로 `id@yeungnam.hs.kr`을 조립하므로
**그 계정 하나만** 권한을 받는다. 다른 계정으로 로그인하면:

| 기능 | 결과 |
|---|---|
| 로그인 · 시간표 · 급식 · **연락처 조회** | ✅ 정상 (도메인만 봄) |
| 성적 전체열람 · 담임 반 성적 · 학부모 상담 | ❌ 안 됨 (이메일 대조) |
| 메모 · 할 일 | ⚠️ 빈 화면 (`uid` 기준이라 별개 저장소) |

**왜 미뤘나** — 아무도 겪었다고 말한 적이 없다. 겪지 않은 문제를 미리 만들 이유가 없다.

**중요**: 규칙에서 **이름으로 매칭하면 안 된다.** 토큰의 `request.auth.token.name`은
구글 프로필 표시 이름이고 **대체로 본인이 바꿀 수 있다.** 그러면 아무 교사나 이름을
바꿔 남의 권한을 가져갈 수 있다. 이름은 '준비 단계'(`acl/emailByName`)에서만 쓰고
'판단 단계'에는 넣지 않는다.

- **하려면**: `acl/emailAliases` = `{ 부계정: 주계정 }` 문서를 두고 **`sync-grades`
  액션이 읽어서** `homerooms`·`managers`에 두 이메일을 다 넣는다.
  **규칙은 손대지 않는다** — 구멍이 안 생기고 평가 비용도 안 는다.
  콘솔에서 직접 `gradeRoles`를 고치는 건 소용없다. 액션이 매번 통째로 덮어쓴다
- **메모·할 일은 `uid` 기준이라 별칭으로 못 붙인다.** "앱은 이 계정으로" 정하는 수밖에
- **예상**: 30분
- **재검토 신호**: "성적이 안 보인다" / "내 반 상담이 안 뜬다"는 문의

### `upload.html` 관리자 확인 — **미룸**

지금은 **영남고 교사면 누구나** 업로드 화면이 열린다. 관리자인지는 확인하지 않고,
저장 버튼을 눌러야 그제서야 `Missing or insufficient permissions`가 뜬다.

**왜 미뤘나** — 규칙이 마지막에 막아 주므로 **데이터는 안전하다.** 저장이 한 배치라
반쪽만 써지는 일도 없다. 불편할 뿐이다.

- **하려면**: 로그인 직후 이메일을 확인해 아니면 "이 계정(`xxx@…`)에는 업로드 권한이
  없습니다"라고 명확히 안내
- **예상**: 10분
- **재검토 신호**: 권한 없는 계정으로 헤매는 일이 반복될 때

### 관리자 이메일 목록화 — **미룸**

`pkh910518@yeungnam.hs.kr`이 **세 곳에 하드코딩**돼 있다 —
`firestore.rules`(`isAppAdmin`), `workers/teacher-api.js`, `workers/consult-api.js`.
계정을 바꾸거나 하나 더 두려면 세 군데를 고치고 재배포해야 한다.

- **하려면**: `acl/appAdmins` 문서에 목록을 두되, **지금 이메일은 코드에 남겨 `OR`로
  묶을 것.** 그 문서를 잘못 건드리면 스스로 잠긴다
- **예상**: 30분
- **재검토 신호**: 담당자 교체, 또는 두 번째 관리자가 필요할 때

---

## 기능 — 안 하기로 한 것

### 상담기록 — **안 하기로 함**

동료 교사가 이미 만들어 쓰고 있다. 중복이다.

### 출결 — **안 하기로 함**

NEIS와 **이중 입력**이 된다. 교사가 같은 걸 두 번 넣게 만드는 기능은 안 쓰인다.

### 학부모상담 현황 대시보드 — **안 하기로 함**

담임별 상담 진행률을 한눈에 보여주는 화면. 만들어 본 뒤 접었다 —
**"감시당하는 느낌"**이 든다. 관리 목적이 아니라 업무 도구여야 한다.

빈 화면으로 두는 편이 낫다는 결론.

---

## 남은 정리거리

일이 잘못돼서가 아니라, 하다 만 것들.

### 워커 배포가 손 붙여넣기다

`consult-api` · `teacher-api` 를 Cloudflare 대시보드에 코드를 붙여넣어 올리고 있다.
저장소의 파일과 실제 돌아가는 코드가 **어긋나 있어도 알 방법이 없고**, 되돌리기도 수동이다.
`workers/README.md` 에 `npx wrangler deploy` 가 이미 적혀 있으니 원래 되던 길이다.

`wrangler.toml` 을 두고 GitHub Actions 를 붙이면 **병합할 때마다 자동 배포**된다.
한 번만 하면 되는 준비는 Cloudflare 에서 API 토큰을 발급해 저장소 시크릿에 넣는 것.

위 '웹푸시' 2단계에서 앱과 워커가 시간표 로직을 **한 벌로 공유**하려면 워커에 빌드
단계가 필요한데, 그 전제가 이것이다. 붙여넣기로는 `import` 를 쓸 수 없다.

- **예상**: 1시간
- **재검토 신호**: 워커를 두 번 이상 연달아 고칠 일이 생길 때, 또는 웹푸시를 시작할 때

### 학급별 보기에 '운영변경' 꼬리표가 안 붙는다 — **안 하기로 함**

교사별 보기는 `_sub`(실제로 바뀐 칸)를 보고 꼬리표를 붙이는데, 학급별 보기의 `_cDev`
는 아직 `ttWeekHasDevGrade` → `ttDayCross` 라 **다른 요일에서 당겨온 날에만** 붙는다.
동아리를 7→6교시로 옮기는 같은 날 안에서의 이동에는 안 붙는다.

**칸 내용은 맞다** — `renderClass` 는 `ttResolveClass` 를 직접 부른다. 꼬리표만 없다.

**왜 안 했나** — 학급별 보기를 여는 상황이 제한적이라 실익이 작다고 판단했다.

- **하려면**: `_cDev` 도 `_sub` 기준으로. 교사별과 같은 방식이 된다 (20분)
- **재검토 신호**: 학급별 보기를 상시로 쓰게 되면

### 실제 권한으로 보기(impersonate)를 다른 페이지에도

`career.html` · `susi.html` · `jeongsi.html` · `ipgyeol.html` · `subject-sim.html`은
관리자의 '실제 권한으로 보기'가 적용되지 않는다. 앱 본체에만 있다.

### 상담 슬롯이 한 문서에 계속 쌓인다

`consultations/{반}`의 `slots` 배열에 지난 학기 것까지 누적된다. Firestore 문서는
1MB 한계가 있어 언젠가 부딪힌다. `findMine()`이 지난 날짜를 걸러 주고 있어 동작에는
지장이 없지만, 학기 단위 아카이브가 필요하다.

### 사진 업로드에 문서 크기 사전 점검

`upload.html`의 사진명렬 업로드는 크기를 재지 않고 `setDoc` 한다. Firestore 문서
한계는 1MiB인데, 150×200 q0.62 기준 한 장 8~14KB라 35명이면 300~500KB다. 아직
여유가 있지만 `{ merge: true }` 라 **재업로드할 때마다 누적**되고, 넘으면
`invalid-argument` 로 던져져 화면에는 "업로드 실패"만 뜬다 — 선생님이 원인을 알 수 없다.
저장 전에 `JSON.stringify(...).length` 로 재서 임계치(예: 800KB)를 넘으면 나눠 저장하거나
압축률을 낮추면 된다.

### 특별실 목록 실수 삭제 — **안 하기로 함**

`⚙️ 특별실 관리`에서 방을 지우고 저장하면 **확인 절차 없이** 전 교사 화면에서 사라지고,
그 방의 기존 예약도 표시될 자리를 잃는다. 다시 추가해도 내부 식별자가 새로 생겨
옛 예약과 연결되지 않는다. 규칙상으로도 `roomBookings/_config` 가 예약 문서와 같은
조건이라 교사 누구나 덮어쓸 수 있다(화면 버튼은 관리자에게만 보인다).

**왜 안 했나** — 상시로 예약이 많은 편이 아니라 실수해도 영향 범위가 작다.

- **하려면**: 삭제 시 "이 방에 예약 ○건이 있습니다" 확인 + 규칙에서 `_config` 만
  `isAppAdmin()` 으로 분리 (30분)
- **재검토 신호**: 특별실 예약이 일상적으로 쓰이기 시작하면

### 사이트 접속 감시

배포 중 잠깐씩 끊기는 것 말고 실제 장애를 알 방법이 없다. GitHub Actions로 10분마다
주소를 확인하고 **2회 연속 실패일 때만** 이슈를 열게 하면 오탐 없이 알 수 있다.
외부 감시 서비스를 쓰면 설정이 저장소 밖에 남아 나중에 "뭘 걸어놨더라"가 된다.

### AHK 설치 프로그램 오류 안내

압축을 풀지 않고 실행해도 설치가 진행되다 어긋나는 경우가 있다. 실패 메시지를
"압축을 먼저 풀어 주세요"로 잡아 주면 문의가 줄어든다.

---

## 코드가 아닌 것 — 실제로 더 효과가 큰 것들

여기까지의 어떤 항목보다 실효성이 있다고 보는 것들.

### 퇴직·전보 계정 정리

**앱 코드로 할 수 있는 일이 아니다.** 보안 규칙은 전부
`@yeungnam.hs.kr` 계정이 살아있는지에 기대고 있다. Google Workspace 관리콘솔에서
계정이 죽으면 모든 규칙이 함께 막힌다. 반대로 살아 있으면 아무리 규칙을 좁혀도
그 사람은 계속 들어온다.

### 사진명렬 원본 엑셀 보관

Firestore는 지금 요금제에서 **시점 복구(PITR)가 안 되고 문서 휴지통도 없다.**
사진이 지워지면 복구 방법은 명렬을 다시 올리는 것뿐이다. 원본 파일만 있으면
최악의 경우에도 되돌릴 수 있다.

### `accessLogs` TTL 정책 — **안 하기로 함**

Firebase 콘솔 → Firestore → **TTL** → 컬렉션 `accessLogs`, 필드 `expireAt`.
설정은 1분이고 코드는 이미 준비돼 있다(`LOG_KEEP_DAYS = 400`, 문서마다 `expireAt`).

**왜 안 했나 — 용량이 문제가 아니라서.** 문서 하나가 `사람 × 날짜` 이고 내용이 짧다
(`target` 이 `S 1-3-7` · `T 김민준` 수준). 문서 구조에서 계산하면 하루 20명 조회에
약 3KB, 상한(100명)까지 써도 13KB다. 교사 한 명이 **매일** 20명씩 찾아본다고 쳐도
1년 0.6MB, 교사 60명이면 연 40MB 정도 — Firestore 무료 한도 1GiB 에 한참 못 미친다.
몇 년 쌓여도 안 찬다.

**남는 찜찜함은 용량이 아니라 내용이다.** `items` 에 "누가 누구 연락처를 언제 봤는지"가
그대로 있다. 연락처 유출을 막으려고 만든 기록이 무한정 쌓이면 지키려던 것과 반대가 된다.
그리고 코드는 **400일 보관을 선언해 두었는데 TTL 이 없으면 실제로는 영구 보관**이다 —
방침과 실제가 어긋나는 상태다. 디스크가 아니라 이쪽이 진짜 이유다.

- **하려면**: 위 콘솔 경로에서 켜기만 하면 된다. 코드 변경 없음 (1분)
- **재검토 신호**: 개인정보 감사·점검이 예고되거나, 접속기록 보관기간을 학교
  개인정보 보호책임자와 맞추게 될 때. 아래 '접속기록 보관기간' 항목과 같이 볼 것

### 접속기록 보관기간을 학교 방침과 맞추기

개인정보 보호법상 접속기록 보관·점검 의무가 있다. 지금은 400일로 잡아 뒀는데,
실제 신고·감사 대응은 학교 **개인정보 보호책임자**와 한 번 맞춰 보는 편이 좋다.
