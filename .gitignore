# 공유받은 원본 소스 (타인 저작물 — 공개 저장소에 올리지 않음)
_grades_src/

# 엑셀 원본 (학생 개인정보 포함 — 절대 커밋 금지. 앱의 업무자료 [⬆ 갱신]으로 Firestore에 업로드)
*.xlsx
*.xls

# 위젯 빌드 산출물 (CI가 릴리스로 배포 — 저장소엔 커밋하지 않음)
widget/*.exe

# 테스트용 devDependencies (tests/README.md 대로 그때그때 npm i)
node_modules/
package-lock.json
package.json
